const initState = {
  parentGroup: null,
  groupName: "",
  messages: []
};

export default function groupReducer(state = initState, action) {
  switch (action.type) {
    default:
      return initState;
  }
}
